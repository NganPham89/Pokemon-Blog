<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon Blog</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header class="bg-light">
        <section class="p-3">
            <h1>Pokemon Blog</h1>
            <div class="display-flex">
                <a href="/" class="p-2 text-primary">Home</a>
                <a href="/dashboard" class="p-2 text-primary">Dashboard</a>
                {{#if logged_in}}
                <button class="ml-2 no-button" id="logout">Logout</button>
                {{else}}
                <a href="/login" class="p-2 text-primary">Login</a>
                {{/if}}
            </div>
        </section>
    </header>
    <main>
        {{{body}}}
    </main>
</body>

{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}

</html>